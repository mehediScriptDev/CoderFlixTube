export const metadata = {
  title: "CoderFlix - Videos",
  description: "Browse videos on CoderFlix.",
  icons: {
    icon: "/favicon.svg",
  },
};

export default function VideoLayout({ children }) {
  return children;
}
